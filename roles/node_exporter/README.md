# Node exporter role

Installs pinned node_exporter binaries and binds port 9100 only to each host's
Hans tunnel address. The systemd collector is enabled for the VPN, proxy, and
monitoring services.
