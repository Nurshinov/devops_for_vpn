# 2026-09-02

## VPN transport

- Replaced the Hans IPv4-over-ICMP fallback with a single Cloudflare transport
  path.
- Preserved the existing client endpoint and profile semantics:
  `188.120.226.48:11999/UDP`.
- Added a pinned GOST 3.2.6 UDP-over-Relay layer:
  - Moscow listens publicly on UDP/11999.
  - `cloudflared access tcp` carries the Relay stream through
    `nurshinov-vpn.com`.
  - Helsinki accepts the Relay stream on localhost TCP/12000 and forwards
    datagrams to OpenVPN on localhost UDP/11999.
- Restored the Helsinki OpenVPN network to `10.0.0.0/24` on `tun0` and updated
  its NAT and forwarding rules.
- Kept the Cloudflare Zero Trust published application route as
  `TCP -> localhost:12000`.

## Automation and cleanup

- Added `install_cloudflare_transport.yml` and the self-contained
  `cloudflare_transport` role.
- Pinned cloudflared and GOST downloads with SHA-256 verification.
- Stored the Cloudflare Tunnel token in an encrypted Ansible Vault variables
  file and ignored the local Vault password file.
- Updated GitHub Actions to deploy only the Cloudflare transport and monitoring
  playbooks.
- Removed the obsolete Hans, nginx UDP proxy, and standalone OpenVPN roles and
  their old installation playbooks.
- Removed the obsolete Moscow node_exporter deployment.

## Monitoring and validation

- Updated VictoriaMetrics and Grafana provisioning for the Helsinki-only
  node_exporter and the `tun0` interface.
- Added GOST backend health to the VPN dashboard.
- Verified four simultaneous existing OpenVPN clients on iOS and Android,
  successful TLS/data-channel establishment, address allocation, and live
  traffic through the new path.
- Verified all transport services and sockets on both VMs.
- Verified Ansible idempotence with `changed=0` and `failed=0` on both hosts.
- Diagnosed the Moscow boot-time `networking.service`/`ifup@ens3` race caused
  by the ISPmanager firewall hook and cleared its stale failed state without
  restarting the active interface.
